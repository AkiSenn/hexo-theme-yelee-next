node_modules/
public/
db.json
*.log
.DS_Store
Thumbs.db
.dev/
.vscode/
.idea/

# CI 本地复现用的临时站点
.ci/

# 主题零依赖，不需要锁文件（防误提交 npm install 产物）
package-lock.json

# 本地交接记录（给新对话读，不进公开仓库）
.handoff.md

# Windows 回收站：某些删除操作会在仓库内生成 $RECYCLE.BIN/，
# 没被跟踪但会被 git add -A 扫进去（tools/sync.mjs 的文件清单同样继承本文件）
$RECYCLE.BIN/
